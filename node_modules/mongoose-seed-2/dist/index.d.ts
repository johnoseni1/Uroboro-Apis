export declare class Seeder {
    private mongoUrl;
    private options;
    constructor(mongoUrl: string, options?: {
        useNewUrlParser: boolean;
        useCreateIndex: boolean;
    });
    private connect;
    private validateModels;
    clearModels(modelNames: string[]): Promise<void>;
    populateModels(seedData: Record<string, object[]>): Promise<void>;
    disconnect(): Promise<void>;
}
